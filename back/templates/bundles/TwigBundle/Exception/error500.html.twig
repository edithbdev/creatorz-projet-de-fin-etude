{% extends 'base.html.twig' %}

{% block title %}Error 500{% endblock %}

{% block body %}
<img src="{{ asset('images/oops.png') }}" alt="page 500" class="d-inline-block align-text-top">
    <h1>Connexion au serveur difficile</h1>
    <p>Le serveur Web rencontre actuellement une difficulté pour trouver la page demandée. Merci de réessayer dans quelques minutes.</p>
    {# <p>Le code est : {{status_code}}</p>
    <p>Le texte est : {{status_text}}</p> #}
    {# {{exception.message}} #}
    <a class="redirect" href="{{ relative_path('http://creatorz.surge.sh/') }}">
				Visitez le site Creatorz'</a>
{% endblock %}